## Usage
For generating every new rhythmic pattern, the numbers in the MS.txt need to be changed according to the pattern of interest. 
The matlab code needs to be adjusted if durations corresponding to each number needs to change (e.g., irregular patterns instead of regular/beat patterns).